<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>{{fontName}}</title>
  <link rel="stylesheet" href="../node_modules/bootstrap/dist/css/bootstrap.min.css"/>
	<style>
		body {
			font-family: sans-serif;
			margin: 0;
			padding: 10px 20px;
		}
		.preview {
			line-height: 2em;
		}
		.preview__icon {
			display: inline-block;
			width: 32px;
			text-align: center;
		}
		{{baseSelector}} {
			display: inline-block;
			font-size: 32px;
		}
		{{{styles}}}
	</style>
</head>
<body>
	<h1>{{fontName}}</h1>
  <table class="table table-hover">
  <thead>
    <tr>
      <th scope="col">图标</th>
      <th scope="col">中文名称</th>
      <th scope="col">英文名称</th>
      <th scope="col">codepoint</th>
    </tr>
  </thead>
  <tbody>
    {{#each names}}
    <tr>
      <td><i class="{{removePeriods ../baseSelector}} {{../classPrefix}}{{this}}"></i></td>
      <td>{{getCh this}}</td>
      <td>{{removeCh this}}</td>
      <td>
        <input type="text" value="{{../codepoint}}"/>
      </td>
    </tr>
    {{/each}}
  </tbody>
</table>
</body>
</html>